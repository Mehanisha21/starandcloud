{
  "name": "vendor-portal",
  "version": "1.0.0",
  "description": "Vendor Portal for CloudWithStar",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend_auth && node server.js",
    "frontend": "cd frontend && ng serve"
  },
  "dependencies": {
    "@angular/common": "^15.0.0",
    "@angular/core": "^15.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
